export function ConfirmCard({ data, onConfirm, onCancel, disabled = false }: { data: Record<string, unknown>; onConfirm:()=>void; onCancel:()=>void; disabled?: boolean }) { const entries=Object.entries(data).filter(([,v])=>v!==null&&v!==''&&!(Array.isArray(v)&&!v.length)); return <section className="confirm-card"><span className="eyebrow">REVIEW BEFORE SAVE</span><h3>Extracted interaction</h3><dl>{entries.map(([k,v])=><div key={k}><dt>{k.replace(/_/g,' ')}</dt><dd>{Array.isArray(v)?v.join(', '):String(v)}</dd></div>)}</dl><div className="card-actions"><button className="primary" onClick={onConfirm} disabled={disabled}>Confirm & save</button><button className="secondary" onClick={onCancel} disabled={disabled}>Discard</button></div></section> }
